// app.js
const input=document.getElementById('tarea');
const btn=document.getElementById('agregar');
const lista=document.getElementById('lista');

function render(){ /* temporal */ }
btn.addEventListener('click',()=>{/* temporal */});
